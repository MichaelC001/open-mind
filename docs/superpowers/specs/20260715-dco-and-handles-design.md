# DCO + launch-surfaces sweep — design

Date: 2026-07-15. Two small pre-flip items, user-approved.

1. **DCO (not CLA).** Contributions are accepted under the Developer
   Certificate of Origin: every commit carries `Signed-off-by` (`git commit
   -s`). Wire-up: a "Sign-off" section in CONTRIBUTING.md linking
   developercertificate.org, and a flip-checklist item to install the DCO
   GitHub App (probot/dco) after the repo is public so PRs are gated
   automatically. Rationale: near-zero contributor friction; the PRD's cloud
   model runs under AGPL as-is and needs no relicensing rights.
2. **Social-handle + domain sweep** — closes the name-sweep report's open
   item: availability of `openmind`/`openmindapp`-style handles (X/Twitter,
   Reddit, Product Hunt, Mastodon) and domains (openmind.app/.dev/.so etc.);
   appended to `docs/20260715-openmind-name-sweep.md`. Research only, no
   registrations.

Verification: CONTRIBUTING renders sensibly; sweep addendum committed; both
public-safe (no sensitive strings).
